#include <iostream>
#include <vector>
using namespace std;
int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		int a, b,cont=0;
		cin >> a >> b;
		for (a; a <= b; a++)
		{
			int c = a % 10;
			if (c == 2 || c == 3 || c == 9)
			{
				cont++;
			}
		}
		cout << cont << endl;
	}
}
